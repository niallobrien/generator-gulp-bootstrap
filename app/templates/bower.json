{
  "name": "<%= _.slugify(appname) %>",
  "private": true,
  "dependencies": {<% if (includeBootstrap) { %>
    "bootstrap-sass-official": "~3.1.1",
    <% } if (includeModernizr) { %>
    "modernizr": "~2.6.2",<% } %>
    "jquery": "~1.11.0"
  },
  "devDependencies": {}
}
